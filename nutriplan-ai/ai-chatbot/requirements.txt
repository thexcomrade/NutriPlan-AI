# Core language processing
nltk==3.8.1
regex==2023.12.25

# YAML parsing for intents
PyYAML==6.0.1

# Language detection and translation
langdetect==1.0.9
googletrans==4.0.0-rc1

# Optional OpenAI integration
openai==1.30.1

# Data structures and utilities
python-dateutil==2.9.0.post0
pytz==2024.1

# Logging and diagnostics
loguru==0.7.2

# Optional for backend integration / chatbot API exposure
Flask==3.0.3
flask-cors==4.0.0

# Optional OCR (for prescription scanning if used)
pytesseract==0.3.10
Pillow==10.3.0

# Optional voice input (for future integration)
SpeechRecognition==3.10.0
pyaudio==0.2.14 ; platform_system=="Windows"

# Optional for PDF/Doc parsing (if prescription is in file format)
pdfplumber==0.10.3
python-docx==1.1.0

# Compatibility and versioning
certifi>=2024.6.2
