# Web framework and server
fastapi==0.100.0
uvicorn[standard]==0.22.0

# Data validation and parsing
pydantic==2.1.1

# Environment variable management
python-dotenv==1.0.0

# Cross-Origin Resource Sharing support middleware
fastapi-cors==0.1.2

# For handling file uploads (multipart/form-data)
python-multipart==0.0.6

# Logging utilities (optional, for better log management)
loguru==0.7.0

# Async HTTP client (useful for calling external APIs, if needed)
httpx==0.24.1

# Database ORM (example: if you use SQLAlchemy for persistence)
sqlalchemy==2.0.19
databases==0.6.1  # async database support

# Security libraries (JWT tokens, hashing, OAuth2)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# AI / ML backend dependencies (if applicable)
numpy==1.26.4
pandas==2.0.3
scikit-learn==1.3.0
tensorflow==2.13.0  # or torch==2.0.1 (choose based on your AI model backend)

# Utility libraries for JSON and data handling
orjson==3.9.8

# Optional: Redis for caching/session management (if your app uses it)
redis==4.6.0
aioredis==2.0.1

# Optional: Email sending support (if you handle emails)
fastapi-mail==1.5.6

# Optional: For improved API docs generation and swagger UI themes
fastapi-versioning==0.12.0

# Optional: To support date/time parsing & formatting
python-dateutil==2.8.2

# Optional: For input validation enhancements
email-validator==2.0.0

# Optional: Security HTTP headers middleware
secure==0.2.1

# Optional: For API rate limiting (if implemented)
slowapi==0.1.6

# Optional: For password and token generation
bcrypt==4.0.1

# Optional: For improved concurrency (if needed)
anyio==3.7.1

# Optional: For API testing (if you have test cases)
pytest==7.4.0
httpx==0.24.1


'''Explanation:
1.Core API: fastapi, uvicorn with standard extras for performance.
2.Validation & parsing: pydantic is core for FastAPI models.
3.Env vars: python-dotenv to manage .env config files.
4.CORS middleware: fastapi-cors or use built-in FastAPI CORS (you may remove the external if you only use built-in).
5.Multipart: For handling file uploads.
6.Logging: loguru for better logging support.
7.HTTP Client: httpx to make async API calls.
8.DB: sqlalchemy + databases for async DB connections if you store data.
9.Security: JWT with python-jose, password hashing with passlib.
10.AI/ML: numpy, pandas, scikit-learn, and either tensorflow or torch depending on what you use.
11.JSON: orjson for fast JSON serialization (optional).
12.Redis: For caching or session management (optional).
13.Email: For email notifications.
14.Versioning & rate limiting: For advanced API features.
15.Date & validation utilities: For better input processing.
16.Testing: pytest and httpx for API tests.
'''
